<?xml version="1.0" encoding="UTF-8" ?>
<!--
  Cyclone DDS peer-list template (LEGACY — ADR 0004).

  This config was used when inter-drone communication ran over Cyclone DDS on
  a shared WiFi LAN. It is superseded by ADR 0009: hardware drones now use
  SiK/RFD900 MAVLink radio for cross-drone traffic and `cyclonedds_local_only.xml`
  for DDS, which binds to `lo`. This file is retained as a fallback for
  bench-up over WiFi when radios are not yet available; do NOT use it on
  airframes.
-->
<CycloneDDS xmlns="https://cdds.io/config" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="https://cdds.io/config https://raw.githubusercontent.com/eclipse-cyclonedds/cyclonedds/master/etc/cyclonedds.xsd">
  <Domain id="any">
    <General>
      <NetworkInterfaceAddress>wlan0</NetworkInterfaceAddress>
      <AllowMulticast>spdp</AllowMulticast>
      <EnableMulticastLoopback>true</EnableMulticastLoopback>
      <MaxMessageSize>65500B</MaxMessageSize>
    </General>
    <Discovery>
      <ParticipantIndex>auto</ParticipantIndex>
      <Peers>
        <!-- Replace with actual swarm IPs -->
        <Peer address="192.168.42.10"/>  <!-- drone_0 (leader) -->
        <Peer address="192.168.42.11"/>  <!-- drone_1 -->
        <Peer address="192.168.42.12"/>  <!-- drone_2 -->
        <Peer address="192.168.42.13"/>  <!-- drone_3 -->
        <Peer address="192.168.42.14"/>  <!-- drone_4 -->
        <Peer address="192.168.42.1"/>   <!-- gcs -->
      </Peers>
    </Discovery>
    <Internal>
      <Watermarks>
        <WhcHigh>500kB</WhcHigh>
      </Watermarks>
    </Internal>
  </Domain>
</CycloneDDS>
